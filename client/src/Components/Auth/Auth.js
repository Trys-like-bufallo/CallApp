import axios from 'axios';

const Auth = () => {

    if(localStorage)


    return (
        <div className = "login">
            <h1 className = "login-title">Login</h1>
        </div>
    )
}

export default Auth;